{% extends 'base.html.twig' %}

{% block title %}Hello {{ user.username }}!{% endblock %}

{% set include_navigation_bar = true %}

{% block body %}
    <div class="container">
        {{ form(form) }}
    </div>

    <div class="col-12 text-end">
        <a href="{{ path('app_profile') }}" class="btn btn-primary">Return Back</a>
    </div>
{% endblock %}
